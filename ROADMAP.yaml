areas:
  functionality:
    forms:
      - create post
    pages:
      - profile
    refactoring:
      - remove ../Component.js
      - use <> or <Fragment>

  bugs:
    - one can't create threads with the same
      name but in different section

  appearance:
    - nice consistent links
    - good-looking forms
    - loading animation

  compatibility:
    - mobile version